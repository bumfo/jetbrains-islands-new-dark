# CLAUDE.md

- `.icls` files are IntelliJ XML color scheme format. The `parent_scheme` is Darcula — only overrides are stored.
- `Islands_New_Dark.icls` is the actively developed theme; `Islands_Dark.icls` is the reference baseline.
